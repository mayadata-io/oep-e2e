test_name: change-role-check
director_ip: "{{ lookup('env','DIRECTOR_IP') }}"
group_id: "{{ lookup('env','GROUP_ID') }}"
role: "{{ lookup('env','ROLE') }}"
member_emailId: "{{ lookup('env','MEMBER_EMAIL_ID') }}"
